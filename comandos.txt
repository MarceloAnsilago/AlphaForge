python -m venv venv

Windows PowerShell:
.\venv\Scripts\Activate.ps1

Windows CMD:
venv\Scripts\activate.bat

Linux/Mac:
source venv/bin/activate

Instalar dependencias:
pip install -r requirements.txt

Abrir a interface:
streamlit run app.py

Abrir a interface desktop:
python desktop_app.py

Abrir a interface desktop por atalho/launcher:
.\AlphaForge Desktop.bat

Rodar os testes:
pytest -q

Executar minerador por CLI com backend local:
python workers\miner_worker.py --dataset .\data\candles.csv --db auto
